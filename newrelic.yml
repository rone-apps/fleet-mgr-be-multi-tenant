common: &default_settings
  license_key: ${NEW_RELIC_LICENSE_KEY}
  app_name: ${NEW_RELIC_APP_NAME}
  distributed_tracing:
    enabled: true
  logging:
    level: info

  rules:
    ignore_url_regexes:
      - ".*/actuator/.*"
      - ".*/health"

